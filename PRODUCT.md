# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 校领导：在 PC 端快速判断全校碳排放状态、风险、趋势与优先治理对象。
- 后勤能源管理人员：在 PC 端持续监控能源负荷、设备、仪表与告警，并定位需要处置的异常。
- 碳管理人员和数据填报人员：维护功能面板中的业务数据，并要求驾驶舱与功能面板同步后保持一致。

## Product Purpose

高校智慧碳管理平台把校园建筑、能源、排放、设备、仪表、告警和治理数据汇集到统一工作流中。驾驶舱用于快速形成态势认知和行动优先级；功能面板用于录入、核算、诊断、处置和追溯。成功意味着驾驶舱展示与功能面板的同源数据一致，并能在数据类别、数量和时间范围变化后继续正确显示。

## Positioning

平台以真实校园空间为主索引，把建筑级碳排放与能源运行状态叠加到同一校园地图，并分别为领导决策和后勤处置提供不同信息层级。

## Operating Context

- 系统入口路由为 `/`，提供账号密码登录界面以及领导舱、后勤舱和 PC 端三个演示入口。
- 领导组驾驶舱路由为 `/leader`，中心为校园 2.5D 地图，左右为决策指标与风险面板。
- 后勤组驾驶舱路由为 `/operations`，中心为校园 2D 地图，左右为实时运行、设备、仪表与告警面板。
- 两个驾驶舱以 PC 和大屏横向布局为主，并需要覆盖常见笔记本、16:9、21:9 和高分辨率桌面。
- 驾驶舱数据当前来自 TypeScript 演示数据，后续将与功能面板或 API 同步。

## Capabilities and Constraints

- 保留现有 Next.js 16、React 19、TypeScript、Tailwind CSS 4、Recharts、ECharts、MapLibre、Zustand 和 Framer Motion 技术栈。
- 保留两个驾驶舱的地图交互、面板折叠、趋势抽屉、主题系统和页面路由。
- 所有指标、图表、排名、比例和摘要必须由输入数据推导，不得把业务数值、类别数量或数据长度写死在可视化组件中。
- 数据为空、仅一个点、类别增减、极值相同、负值、长标签和大数值时都应有可读的降级行为。
- 预测值与实绩必须有明确的非颜色区分；无数据时不得显示误导性的零。
- 当前数据为演示数据，页面必须保留“Demo 模拟数据，不用于申报”提示。

## Brand Commitments

- 产品名称为“高校智慧碳管理平台”。
- 保留专业、数据驱动、科技环保、可信赖的产品语气。
- 校园地图和真实建筑空间是产品识别度最高的既有资产，不用装饰插画替代。

## Evidence on Hand

- 两个现有驾驶舱实现：`src/app/page.tsx`、`src/app/operations/page.tsx`。
- 驾驶舱数据模型：`src/data/leader-dashboard-data.ts`、`src/data/operations-data.ts`。
- 地图、建筑和交互组件：`src/components/dashboard/`。
- 现有设计规范：`DESIGN.md`。
- 当前截图：`output/playwright/leader-outer-default.png`、`output/playwright/operations-default-11.png`。
- 没有真实生产数据、用户研究结果或正式指标口径文档；不得据此编造申报或节能成效。

## Product Principles

1. 同一数据源，同一口径：驾驶舱与功能面板必须能共享数据模型。
2. 地图承担空间定位，侧栏承担判断与行动，二者不争夺注意力。
3. 领导视图先给结论和风险，后勤视图先给状态、异常和处置顺序。
4. 数据变化是常态：可视化默认适配可变数量、可变范围和空状态。
5. 视觉表现服务快速扫描，不以装饰性的科技感牺牲可读性。

## Accessibility & Inclusion

- 交互必须支持键盘焦点、清晰标签和减少动效偏好。
- 状态不能只依赖红绿颜色区分，需要同时使用文字、形状、线型或图标。
